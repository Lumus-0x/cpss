# ============================================
# CPSS - Cross-Platform Sync System
# Конфигурация через переменные окружения
# Скопируйте этот файл в .env и заполните значения
# ============================================

# --------------------------------------------
# СЕТЕВЫЕ НАСТРОЙКИ
# --------------------------------------------
# IP адрес для привязки сервисов
# 0.0.0.0 - все интерфейсы
# 78.107.254.30 - внешний IP
# 192.168.88.50 - локальный IP
HOST_IP=0.0.0.0

# Порт для Frontend (веб-интерфейс)
# Доступ: http://78.107.254.30:FRONTEND_PORT
FRONTEND_PORT=1800

# Порт для Backend API
# Доступ: http://78.107.254.30:BACKEND_PORT/api
BACKEND_PORT=1800

# Порт для PostgreSQL (внешний доступ опционален)
POSTGRES_PORT=1543

# Порт для Redis (внешний доступ опционален)
REDIS_PORT=1637

# --------------------------------------------
# БЕЗОПАСНОСТЬ
# --------------------------------------------
# Пароль администратора (для входа в веб-интерфейс)
# Используйте надежный пароль!
ADMIN_PASSWORD=your_secure_password_here

# Секретный ключ для JWT токенов
# Сгенерируйте случайную строку: openssl rand -hex 32
SECRET_KEY=your_secret_key_here_change_in_production

# Разрешенные источники для CORS (через запятую)
# Добавьте все IP/домены, с которых будет доступ
CORS_ORIGINS=http://78.107.254.30:1800,http://192.168.88.50:1800,http://localhost:1800

# --------------------------------------------
# БАЗА ДАННЫХ
# --------------------------------------------
DB_USER=cpss_user
DB_PASSWORD=secure_db_password

# --------------------------------------------
# БОТЫ
# --------------------------------------------
# Токен Telegram бота (получить у @BotFather)
TELEGRAM_TOKEN=your_telegram_bot_token

# Токен Discord бота (получить в Discord Developer Portal)
DISCORD_TOKEN=your_discord_bot_token

# --------------------------------------------
# FRONTEND НАСТРОЙКИ
# --------------------------------------------
# URL для API (используется если фронтенд и бэкенд на разных хостах)
# По умолчанию /api (относительный путь)
REACT_APP_API_URL=/api

# Полный URL бэкенда для фронтенда (если нужен абсолютный путь)
REACT_APP_BACKEND_URL=http://78.107.254.30:1800

